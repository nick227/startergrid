# The URL where the authenticated web application (dealer workspace / admin app) is hosted.
# Used for routing users from the public splash site to the auth/signup flows.
VITE_APP_URL=http://localhost:5173
