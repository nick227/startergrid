# Legacy VDP Folder

This folder is compatibility-only during the marketplace component-system migration. Do not add new implementation files here; use `components/listing/detail` for detail presentation and `components/media` for gallery/media UI.

