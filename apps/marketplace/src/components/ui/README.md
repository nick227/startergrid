# Legacy UI Folder

This folder is compatibility-only during the marketplace component-system migration. Do not add new implementation files here; use `components/core`, `components/media`, `components/listing`, `components/seller`, `components/feedback`, or an owning `features/*` folder.

