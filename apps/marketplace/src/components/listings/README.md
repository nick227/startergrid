# Legacy Listings Folder

This folder is compatibility-only during the marketplace component-system migration. Do not add new implementation files here; use `components/listing/*` for presentation and `features/*` for stateful listing workflows.

